apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: ai-chatbot
spec:
  template:
    spec:
      volumes:
      - name: redis-data
        persistentVolumeClaim:
          claimName: redis-data-pvc